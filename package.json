{
  "name": "reshare",
  "description": "A self-hosted resume management and sharing platform powered by Cloudflare Workers.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "deploy": "wrangler deploy"
  }
}
